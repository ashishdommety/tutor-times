/* Controllers include:
      - add note - CREATE
      - get all notes - READ
      - edit note - UPDATE
      - delete note - DELETE
*/

const displayNotes = require('./displayNotes');

module.exports = {
      showAll: displayNotes
}